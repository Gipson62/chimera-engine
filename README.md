# chimera-engine

Requires a C compiler (preferably GCC or Clang) & assimp to built it. The rest should work out of the box? (idk I didn't try).

GCC using MSYS2 UCRT64 is mandatory on Windows (except if you reconfigure the atlas.toml yourself)
